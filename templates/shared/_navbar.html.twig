<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path ('homepage')}}">Symshop</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor03">
			<ul class="navbar-nav me-auto">
				{# Appel d'un controller dans un fichier twig. #}
				{# {{ render(controller('App\\Controller\\CategoryController::renderMenuList'))}} #}

					{% for c in categoryRepository.findAll() %}
						<li class="nav-item"> <a class="nav-link" href="#">{{c.name}}</a>
					</li>
				{% endfor %}
			</ul>
			<form class="d-flex">
				<ul class="navbar-nav">
					<li class="nav-item">
						<a href="#" class="nav-link">Inscription</a>
					</li>

					<li class="nav-item">
						<a href="#" class="btn btn-success">Connexion</a>
					</li>

					<li class="nav-item">
						<a href="#" class="btn btn-danger">Deconnexion</a>
					</li>
				</ul>
			</form>
		</div>
	</div>
</nav>
